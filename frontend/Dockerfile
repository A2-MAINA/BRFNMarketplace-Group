# Use Nginx to serve the static placeholder page
FROM nginx:alpine

# Copy the placeholder HTML into Nginx's default serve directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80
EXPOSE 80